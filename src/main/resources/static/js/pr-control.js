var startUrl = 'http://sg.weibo.com';
var numPages = '100';
var iterRank = '100';
var prControl = new Vue({
	el : '#pr-control',
	data : {
		startUrl : startUrl,
		numPages : numPages,
		iterRank : iterRank
	}
});
